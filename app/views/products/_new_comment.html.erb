<% if signed_in? %>
	<p>Add a review</p>
	<%= form_for([@product, @product.comments.build]) do |f| %>
		<p>
			<%= f.text_area :body %>
		</p>
		<p>
			<div class="rating"></div>
		</p>
		<p>
			<%= f.submit "Submit" %>
		</p>
	<% end %>
<% end %>